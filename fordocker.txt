# building docker image

docker build -t walletbuddyai .

# building docker container with the image that was created
# it is running on localhost:8000

docker run -p 8000:8000 -d walletbuddyai

# gives all created containers

docker ps -a

# gives all the terminal logs that happened

docker logs <container_id>